import { Location, ReferenceParams } from 'vscode-languageserver';
import { PrismaSchema } from '../Schema';
export default function references(schema: PrismaSchema, params: ReferenceParams, onError?: (errorMessage: string) => void): Location[] | undefined;
