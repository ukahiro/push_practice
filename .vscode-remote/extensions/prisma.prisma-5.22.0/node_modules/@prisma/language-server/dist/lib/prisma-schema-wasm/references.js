"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = references;
const internals_1 = require("./internals");
const _1 = require(".");
function references(schema, params, onError) {
    try {
        if (process.env.FORCE_PANIC_PRISMA_SCHEMA) {
            (0, internals_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                _1.prismaSchemaWasm.debug_panic();
            });
        }
        const response = _1.prismaSchemaWasm.references(JSON.stringify(schema), JSON.stringify(params));
        return JSON.parse(response);
    }
    catch (e) {
        const err = e;
        (0, internals_1.handleWasmError)(err, 'references', onError);
        return undefined;
    }
}
//# sourceMappingURL=references.js.map