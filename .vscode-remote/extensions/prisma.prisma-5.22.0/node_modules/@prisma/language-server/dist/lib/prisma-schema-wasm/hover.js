"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = hover;
const _1 = require(".");
const internals_1 = require("./internals");
function hover(schema, initiatingDocument, params, onError) {
    try {
        if (process.env.FORCE_PANIC_PRISMA_SCHEMA) {
            (0, internals_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                _1.prismaSchemaWasm.debug_panic();
            });
        }
        const result = _1.prismaSchemaWasm.hover(JSON.stringify(schema), JSON.stringify(params));
        return JSON.parse(result);
    }
    catch (e) {
        const err = e;
        (0, internals_1.handleWasmError)(err, 'hover', onError);
        return undefined;
    }
}
//# sourceMappingURL=hover.js.map