import { CodeAction, Range, Diagnostic } from 'vscode-languageserver';
import { TextDocument } from 'vscode-languageserver-textdocument';
export declare function assertQuickFix(expected: CodeAction[], textDocument: TextDocument, range: Range, diagnostics: Diagnostic[]): void;
export declare function createDiagnosticErrorUnknownType(unknownType: string, range: Range): Diagnostic;
